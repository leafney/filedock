## 说明

- 当前项目处于开发阶段，如果需要涉及到表结构的修改或者功能逻辑的优化，不需要考虑兼容性，直接修改即可。

## i18n 多语言规则

- 项目所有面向用户的功能必须同时支持简体中文（`zh-CN`）与 English（`en`）。
- 新增或修改页面文案、API 响应消息、参数校验提示、鉴权与权限提示、全局错误提示、SSE 用户提示时，必须在同一变更中补齐两种语言。
- 前端用户可见文案必须使用 i18n 翻译键，禁止在 React 组件中直接硬编码；品牌名、协议常量、机器状态值除外。
- 后端业务逻辑只返回错误码、翻译参数和内部错误原因；最终用户消息由统一响应层根据 `Accept-Language` 生成。
- 新增后端错误码时，必须同时增加 `zh-CN` 与 `en` 翻译，并确保词典完整性测试通过。
- 日志、业务错误码、JSON 字段名、数据库值、用户输入、文件名、版本号、Git 信息等内容不翻译。
- 语言选择只保存在前端 localStorage；后端不得持久化用户语言。
