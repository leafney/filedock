# FileDock 待办事项

## 前端主题

- [ ] 后续实现全站浅色模式与深色模式，补充主题切换入口、用户偏好持久化、Ant Design Token，以及首页雷达、房间详情页和全局组件的双主题适配；当前阶段继续使用浅色主题。

## 配置加载

- [ ] 正式部署前完善配置加载逻辑，包括部署环境下的配置覆盖、校验、敏感信息管理与错误提示。

## 服务健康状态

- [ ] 正式部署前完善 `/version` 接口的 `status` 计算逻辑，加入真实运行状态验证；开发阶段固定返回 `ok`。

## 会话配置

- [ ] 后续管理功能支持管理员修改匿名会话的默认过期时长；当前默认值固定为 30 天，不提供 Refresh Token。
- [ ] 后续管理功能支持管理员重置 JWT 签名密钥；重置前必须二次确认，并明确提示该操作会撤销全部现有会话、使所有浏览器匿名身份失效。

## HTTPS 证书

- [ ] 后续支持自动生成自签名 HTTPS 证书，并补充各平台证书信任与安全警告处理指引；当前阶段只支持读取用户提供的证书和私钥，不自动生成证书。

## 传输并发配置

- [ ] 后续管理功能支持管理员调整上传和下载并发限制；初始默认值固定为上传全局 4、单房间 2、单用户 2，下载全局 50、单房间 10、单用户 2。

## 接口限速配置

- [ ] 后续管理功能支持管理员调整匿名会话、房间信息查询、房间创建、房间加入、房主验证申请、PIN 验证和随机昵称接口的 IP 限速默认值；当前阶段使用代码固定参数。

## 匿名用户昵称

- [ ] 后续管理功能支持管理员新增、删除和调整随机昵称候选项；候选项默认来源包括《水浒传》一百零八将和《三国演义》人物名称。
- [ ] 后续管理功能支持管理员维护昵称黑名单，禁止使用容易冒充系统身份或含有恶意、侮辱含义的名称；当前阶段使用代码内置保留词，例如 `admin`、`管理员`、`系统` 和 `FileDock`。

## 房间标题

- [ ] 后续支持房主把默认标题 `房间 <四位房间号>` 修改为自定义标题；当前阶段创建时写入默认标题，不提供修改接口。

## 房间清理管理

- [ ] 后续管理员页面支持查看所有 `cleanup_pending`、`cleanup_running`、`cleanup_retry_waiting` 和 `cleanup_failed` 房间，并展示清理阶段、尝试次数、计划时间、开始时间、完成时间、各类关联数据总数与已清理数量、最后错误和房间号是否仍被锁定。
- [ ] 后续管理员页面允许对 `cleanup_failed` 房间人工执行一次幂等清理；同时专项设计清理持续失败时安全释放房间号的规则，避免旧数据未隔离完成便直接复用房间号。

## 房间后续协作能力

- [ ] 后续实现房间回收站，包括用户主动删除、房主强制删除、恢复申请与审批、回收站免费额度、是否计入房间容量的管理员开关，以及房间销毁时的最终物理清理。
- [ ] 后续实现房间内一对一聊天，包括成员切换、文字、表情、图片、私密文件卡片、共享文件引用、消息撤回、未读数和服务端已读回执；不实现群聊。

## 全局通知中心

- [ ] 后续实现跨页面共用的全局通知中心，使首页和房间页通知 Icon 使用同一用户级数据源；第一阶段聚合用户自建房间的加入审批，后续扩展私密文件接收、文件传输结果和一对一聊天消息，并统一未读数量、已读状态、SSE 刷新、空状态、通知跳转及权限过滤。本期房间页仍保留当前房间审批通知的过渡逻辑，不新增全局通知接口或通知数据库表。

## 文件可靠性与复用

- [ ] 后续设计并实现可信文件哈希和完整性校验；哈希不得作为私密文件公开标识或直接下载凭证。
- [ ] 后续实现重复文件秒传和物理内容去重，必须同时解决上传者证明、跨权限信息泄漏、容量引用计数和物理文件生命周期问题。
- [ ] 后续实现上传与下载断点续传，包括任务恢复、失效清理和服务重启后的状态重建。
- [ ] 后续实现 HTTP Range 下载与分块下载，并明确分块校验、并发限制、客户端合并和失败重试规则。
