# FileDock 一期收口手动验收

Related PRD: [docs/plan/2026-08-03-phase-one-closure.md](../plan/2026-08-03-phase-one-closure.md)

状态：一期收口验收通过

## 准备

- 用户自行删除 `data` 目录中的旧数据库数据。
- 会话 A：桌面浏览器，房主。
- 会话 B：独立桌面浏览器，成员甲。
- 会话 C：同一局域网真实手机，成员乙。
- 准备一个小文件和一个可观察进度的中等文件。

三个会话必须使用独立 Cookie。Agent 不操作数据库数据。

## 自动检查

由 Agent 填写，不属于用户手动项目。

- 结果：通过
- 日期：2026-08-03
- 摘要：Go 1.25.12 全包测试通过；Bun 1.3.5 前端 6 项测试通过；TypeScript 与 Vite 生产构建通过；`git diff --check` 通过。构建仅有已知大分块警告，主 JavaScript gzip 为 305.34 kB，不阻断一期收口。

## 十个手动场景

### 1. 匿名身份

- [x] A、B、C 分别创建身份，刷新后仍恢复正确身份；修改昵称后相关页面最终同步。

### 2. 开放房间

- [x] A 创建开放房间；B 通过房间号或分享链接加入；双方看到正确成员。

### 3. 受保护房间

- [x] PIN 或房主审批模式任选一个跑完整流程；另一种只检查创建和加入入口可用。

### 4. 成员与 SSE

- [x] A、B、C 同时进入房间，成员和在线状态一致；一个会话离开或关闭后，其他会话看到变化。

### 5. 共享文件

- [x] 上传共享文件；其他成员能看到并原生下载；文件名、状态和进度基本正确。

### 6. 私密文件

- [x] 发送私密文件；接收者完成接收下载和另一次拒绝；未选中的第三方看不到完整信息。

### 7. 复用与容量

- [x] 再次发送已有私密文件，不重新上传内容，房间容量不重复增加。

### 8. 生命周期结束

- [x] 依次验证成员退出、房主踢人、房主解散；相关会话及时更新并停止新操作。

### 9. 手机与 English

- [x] C 在真实手机完成加入、查看文件和一次文件操作；任一会话切换 English 后无明显缺词或布局阻断。

### 10. 减少动态效果

- [x] 开启减少动态效果后，首页持续动画停止，身份和房间入口仍正常。

## 问题

通过时无需填写。失败时追加：场景编号、复现步骤、期望结果、实际结果；修复后追加复验结论。

- 已修复并复验通过：用户主动退出房间后，其他成员能看到成员列表更新，并收到顶部临时消息。
- 已修复并复验通过：新成员首次加入或重新加入时，仅当前房间页的其他成员收到顶部临时消息；新加入者本人不收到，通知 Icon 不记录。
- 已修复并复验通过：用户申请加入房间的确认页面、房主解散房间后的提示页面不再显示黑底，文字与操作已有完整排版。前端变更需要重新执行 `make build` 并重启服务，因为静态资源被编译进 Go 二进制。
- 后续处理，不阻断一期：加入确认页暂未复用通用顶部样式，用户决定后续统一修改。

## 结论

- 验收人：用户
- 验收日期：2026-08-03
- 结果：通过；自动门禁、十个手动场景和验收期间缺陷复验均已完成
- 用户确认：已确认最终手动复验通过
